import{_ as t,o as a,c as o,a8 as n}from"./chunks/framework.BqC7ICod.js";const g=JSON.parse('{"title":"Sentences","description":"randSentence writes a whole statement rather than a name or a handle: a subject, and something said about it. This page is how it decides what may stand where.…","frontmatter":{},"headers":[],"relativePath":"sentence/index.md","filePath":"en/sentence/index.md","lastUpdated":1788842458000}'),s={name:"sentence/index.md"};function d(c,e,r,h,i,l){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="sentences" tabindex="-1">Sentences <a class="header-anchor" href="#sentences" aria-label="Permalink to &quot;Sentences&quot;">​</a></h1><p><a href="./rand-sentence"><code>randSentence</code></a> writes a whole statement rather than a name or a handle: a subject, and something said about it. This page is how it decides what may stand where. For the options themselves, see the <a href="./rand-sentence">reference page</a>.</p><h2 id="the-words-of-one-sentence-belong-together" tabindex="-1">How the verb picks the nouns <a class="header-anchor" href="#the-words-of-one-sentence-belong-together" aria-label="Permalink to &quot;How the verb picks the nouns {#the-words-of-one-sentence-belong-together}&quot;">​</a></h2><p>What keeps the words of one sentence together is the verb. Every verb states which kinds of noun can do it, and, when it is transitive, which kinds it can be done to. The nouns are then drawn from those alone.</p><p>So <code>여우가 사과를 먹는다</code> is a sentence the generator can build and <code>여우가 철학을 먹는다</code> is not: <code>먹는다</code> accepts something edible and nothing else.</p><p>No noun carries a tag for this. The twenty-nine <a href="./../word/themes">themes</a> already say what a word is, so each theme maps to one <strong>noun class</strong>, the same way in every language:</p><table tabindex="0"><thead><tr><th>Class</th><th>Themes</th></tr></thead><tbody><tr><td><code>creature</code></td><td><code>animal</code>, <code>myth</code></td></tr><tr><td><code>person</code></td><td><code>job</code>, <code>person</code></td></tr><tr><td><code>plant</code></td><td><code>plant</code></td></tr><tr><td><code>edible</code></td><td><code>food</code>, <code>drink</code></td></tr><tr><td><code>thing</code></td><td><code>object</code>, <code>tool</code>, <code>clothing</code>, <code>product</code>, <code>gem</code>, <code>music</code>, <code>toy</code>, <code>furniture</code></td></tr><tr><td><code>vehicle</code></td><td><code>vehicle</code></td></tr><tr><td><code>place</code></td><td><code>place</code>, <code>nature</code>, <code>space</code></td></tr><tr><td><code>event</code></td><td><code>weather</code>, <code>sport</code>, <code>time</code>, <code>sound</code></td></tr><tr><td><code>idea</code></td><td><code>concept</code>, <code>emotion</code>, <code>finance</code>, <code>tech</code>, <code>color</code></td></tr><tr><td><code>body</code></td><td><code>body</code></td></tr></tbody></table><p>A verb that says <code>creature</code> and <code>person</code> can be done by a lion and by a locksmith, and by nothing else. That is the whole mechanism, and it is why the sentences read as sentences rather than as filled-in templates.</p><p>Where a class is too wide, a group narrows it to themes on either side of the verb. <code>eat</code> takes <code>food</code> and <code>drink</code> takes <code>drink</code>, though both are <code>edible</code>; <code>익는다</code> is a thing <code>food</code> does and <code>울린다</code> a thing <code>music</code> does, though a soup and a song are both <code>edible</code> and <code>thing</code> to the classes. A theme narrowed out of one group is always accepted by another of the same field, so nothing loses its verbs — a song stopped swaying and started ringing.</p><p>Below the theme, a noun may carry a <strong>trait</strong>: a language lists which of its nouns fly, swim or crawl, and a verb asks for one or rules one out. <code>날아오른다</code> takes a flier, <code>헤엄친다</code> a swimmer, and <code>달린다</code> takes anything that neither swims nor crawls — so a sparrow takes off, a mackerel swims, a snake crawls, and a lion, which is listed nowhere, runs. Every language lists its own: <code>the heron flew</code>, <code>der Basilisk kriecht</code>, <code>el sapo nada</code>. The lists name the exceptions and nothing else, and every noun keeps a verb in every field its kind has. One trait takes everything away: a word of a creature theme that is no creature — a spell, a rune, an amulet, <code>마력</code> — is <code>lifeless</code>, and a sentence about a myth is about the dragons and the elves, never <code>the charm chooses</code>. Another takes a noun out of the places: a word of the <code>place</code> class that is no place — a wave, a comet, a lightyear — is <code>placeless</code>, so a sentence happens in a forest or on a moon and never <code>in the lightyear</code>. A verb may ask the same of its object: in English <code>sips</code> takes a <code>liquid</code>, <code>chews</code> takes none, and <code>roasts</code> and <code>slices</code> take something <code>raw</code>, so a curry is simmered and a steak is grilled, and nobody chews a syrup.</p><p>A place takes the preposition it takes. Every English place frame writes <code>in</code>, and a language may list where that is the wrong word: <code>on the balcony</code>, <code>at the market</code>, <code>under the sky</code>. A place listed nowhere keeps the frame&#39;s own.</p><p>It is not a promise that every sentence means something. A lion can bake a pudding here, and an ocarina can glitter at dusk. What the classes rule out is the sentence that does not parse as a thought at all.</p><h2 id="the-shapes-belong-to-the-language" tabindex="-1">The shapes each language declares <a class="header-anchor" href="#the-shapes-belong-to-the-language" aria-label="Permalink to &quot;The shapes each language declares {#the-shapes-belong-to-the-language}&quot;">​</a></h2><p>Every language writes out the shapes its own grammar allows, in its own word order, with the particle or preposition each phrase needs. Korean closes on its verb, English puts it second, and Chinese frames the action before it:</p><table tabindex="0"><thead><tr><th>Language</th><th>A sentence</th></tr></thead><tbody><tr><td>Korean</td><td><code>검은 고양이가 숲에서 잠잔다.</code></td></tr><tr><td>Japanese</td><td><code>ハンバーガーが暗礁で冷める。</code></td></tr><tr><td>Chinese</td><td><code>巨乌贼又品尝朱红椰汁。</code></td></tr><tr><td>English</td><td><code>The angler cleans the towel in the ivory balcony.</code></td></tr><tr><td>German</td><td><code>Im Frühling blüht eine Chrysantheme noch.</code></td></tr></tbody></table><p>Two consequences follow.</p><p>First, a language declares only what it can write correctly. German has no shape with an object and Russian none with a place, because both would put the noun in a case its own ending has to change for, and the pools hold one form of each word. A request neither can answer falls back to the closest shape they do have, and with no <code>language</code> the ones that can answer are preferred.</p><p>Second, a particle belongs to the phrase rather than to the sentence. Korean picks between <code>가</code> and <code>이</code> by whether the word in front of it closes on a consonant, so <code>사자가</code> and <code>사슴이</code> come out right without either particle being stored twice. Italian picks between <code>l&#39;</code>, <code>lo</code> and <code>il</code> by the sound the noun opens on. German writes its verb second, so a shape that opens on a time puts the subject behind it: <code>Am Morgen schläft ein Wolf.</code></p><h2 id="length-picks-the-shape" tabindex="-1">Length and the shape <a class="header-anchor" href="#length-picks-the-shape" aria-label="Permalink to &quot;Length and the shape {#length-picks-the-shape}&quot;">​</a></h2><p><code>minLength</code> and <code>maxLength</code> bound the whole sentence, punctuation included. The shapes that cannot land inside the range are dropped first, and each phrase is then drawn against the room left once the phrases behind it have reserved their shortest.</p><p>So a narrow range drops a modifier rather than truncating a word, and a wide one puts modifiers back. <a href="./sentence-length-range"><code>sentenceLengthRange</code></a> reports what a language can produce with no bounds at all.</p><p><code>shape</code> is the same idea in coarser units, counting phrases rather than characters. It is usually what a caller means by a short or a long sentence.</p><h2 id="what-realism-changes-and-what-it-does-not" tabindex="-1">What <code>realism</code> reaches <a class="header-anchor" href="#what-realism-changes-and-what-it-does-not" aria-label="Permalink to &quot;What `realism` reaches {#what-realism-changes-and-what-it-does-not}&quot;">​</a></h2><p><code>realism</code> decides where the words come from. At <code>&#39;invented&#39;</code> the nouns and modifiers are built from the language&#39;s own sounds, and the grammar around them does not move: the particles, the articles, the agreement and the shapes are the language&#39;s at every level.</p><p>So the vocabulary of <code>수줍은 노오가 안개 파저멜을 챙긴다.</code> is not Korean, while its grammar is.</p><h2 id="a-story" tabindex="-1">What a paragraph follows <a class="header-anchor" href="#a-story" aria-label="Permalink to &quot;What a paragraph follows {#a-story}&quot;">​</a></h2><p>A result of more than one sentence follows a <strong>story</strong>, and a story is a list of steps. Each step names what the hero does, or what is true of them, and what has to hold before it: <code>eat</code> needs the hero to be holding something, <code>go</code> needs them awake and leaves them away from home, <code>arrive</code> brings them back. A planner walks the steps against those conditions, keeps the ones the story cannot do without, fills the remaining sentences with the optional ones, and drops a step that would contradict what came before it.</p><p>So <code>여우가 시장으로 갔다. 사과를 샀다. 집에 들어와서 사과를 먹었다.</code> is a story the generator can tell and <code>여우가 사과를 먹었다. 사과를 샀다.</code> is not: nobody eats what they have not yet got.</p><p>Three things follow from a step being a step rather than a sentence. The verb of each sentence is drawn from the <strong>field</strong> the step names, <code>buy</code>, <code>eat</code>, <code>arrive</code>, so the sentence says what the step says. The nouns a step puts on the page, the thing and the place, are pinned into every step after it. And two neighbouring steps may be written as one sentence, joined the way the language joins two clauses, where the range has room for both.</p><p><code>randSentence</code> <a href="./rand-sentence#more-than-one-sentence">describes the stories</a>, and <code>tense</code> on the same page is what a story is told in.</p><h2 id="where-a-sentence-differs-from-a-nickname" tabindex="-1">Where a sentence differs from a nickname <a class="header-anchor" href="#where-a-sentence-differs-from-a-nickname" aria-label="Permalink to &quot;Where a sentence differs from a nickname&quot;">​</a></h2><p><a href="./../nickname/rand-nickname"><code>randNickname</code></a> puts words beside each other; a sentence says something about one of them. Three things follow:</p><ul><li>A nickname runs its words together on purpose (<code>멋진사자</code>); a sentence writes the spaces the language writes.</li><li>A nickname has no verb, so it never has to agree with anything. A sentence&#39;s predicate does.</li><li><code>randNickname</code> takes a <code>wordSeparator</code>, because a handle is a string somebody types. A sentence has none — the spacing is the language&#39;s.</li></ul><p>They share the pools, and nothing else.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./rand-sentence"><code>randSentence</code></a> — every option, written out.</li><li><a href="./sentence-length-range"><code>sentenceLengthRange</code></a> — the range the bounds default to.</li><li><a href="./../nickname/">Nicknames</a> — the other generator built on the same words.</li></ul>',36)])])}const m=t(s,[["render",d]]);export{g as __pageData,m as default};
