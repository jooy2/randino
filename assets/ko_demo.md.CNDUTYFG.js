import{_ as c,C as n,o as r,c as t,j as e,a,E as l,a8 as i}from"./chunks/framework.BqC7ICod.js";const k=JSON.parse('{"title":"데모","description":"아래의 모든 것은 브라우저에서 직접 실행됩니다. 컨트롤은 randName, randNickname, randWord, randSentence가 실제로 받는 옵션이고, 결과 아래의 코드 블록은 지금 설정이 만들어 내는 호출이며, 보이는 값은 Generate를 누를 때마다 새로 뽑힙니다.","frontmatter":{},"headers":[],"relativePath":"ko/demo.md","filePath":"ko/demo.md","lastUpdated":1788614611000}'),s={name:"ko/demo.md"};function m(h,o,u,p,g,f){const d=n("Demo",!0);return r(),t("div",null,[o[0]||(o[0]=e("h1",{id:"데모",tabindex:"-1"},[a("데모 "),e("a",{class:"header-anchor",href:"#데모","aria-label":'Permalink to "데모"'},"​")],-1)),o[1]||(o[1]=e("p",null,[a("아래의 모든 것은 브라우저에서 직접 실행됩니다. 컨트롤은 "),e("code",null,"randName"),a(", "),e("code",null,"randNickname"),a(", "),e("code",null,"randWord"),a(", "),e("code",null,"randSentence"),a("가 실제로 받는 옵션이고, 결과 아래의 코드 블록은 지금 설정이 만들어 내는 호출이며, 보이는 값은 Generate를 누를 때마다 새로 뽑힙니다.")],-1)),l(d),o[2]||(o[2]=i('<h2 id="what-to-try" tabindex="-1">이런 것들을 해 보세요 <a class="header-anchor" href="#what-to-try" aria-label="Permalink to &quot;이런 것들을 해 보세요 {#what-to-try}&quot;">​</a></h2><ul><li><code>language</code>를 <code>en</code>으로, <code>realism</code>을 <code>invented</code>로 두어 보세요. 이름은 계속 영어처럼 읽히지만 아무도 쓰지 않는 이름이 됩니다. 풀에서 뽑는 대신 음절 템플릿으로 만들기 때문입니다.</li><li><code>language</code>를 <code>all</code>로 둔 채 Generate를 몇 번 눌러 보세요. 아홉 개의 문자 체계가 나오며, 각 이름의 길이 범위는 묶음이 아니라 그 이름의 언어를 기준으로 정해집니다.</li><li><code>ru</code>를 고르고 <code>gender</code>를 바꿔 보세요. 러시아어는 이 선택이 밖에서 보이는 유일한 언어입니다. 부칭과 성이 모두 성별에 따라 변화합니다.</li><li>닉네임 탭에서 <code>theme</code>을 <code>animal</code>로, <code>wordSeparator</code>를 <code>-</code>로 지정해 보세요. 구분자도 길이에 포함되므로, 범위가 좁으면 단어를 자르는 대신 수식어를 뺍니다.</li><li>영어 닉네임에서 <code>maxLength</code>를 <code>8</code>로 낮춰 보세요. 단어가 잘리는 대신 세 단어짜리 형태가 아예 빠집니다. 길이는 단어가 아니라 형태를 고릅니다.</li><li><code>randSuffix</code>를 켜 보세요. 토큰은 닉네임이 완성된 뒤에 붙기 때문에, 길이 옵션이 그것을 신경 쓸 필요가 없습니다.</li><li>단어 탭에서 <code>theme</code>을 고르고 Generate를 눌러 보세요. 닉네임을 만드는 바로 그 단어 풀을 아무것도 더하지 않은 채로 받게 됩니다. <code>randAnimal</code>을 비롯한 25개 함수가 테마를 미리 정해 둔 이 호출입니다.</li><li>단어 탭에서 장식 함수를 <code>randModifier</code>로 바꿔 보세요. 명사 앞에 수식어를 붙이는 것이 <code>randNickname</code>이 하는 일의 대부분이며, 코드 블록에 그 두 함수가 그대로 드러납니다.</li><li>문장 탭에서 <code>language</code>를 <code>de</code>와 <code>ru</code>로 바꾸고 상세 정보를 켜 보세요. 두 언어에는 <code>object</code>도 <code>place</code>도 없습니다. 둘 다 명사 자체의 어미가 바뀌는 격을 요구하므로, 그 형태는 두 언어가 선언한 목록에 아예 없습니다.</li><li><code>include</code>에 두 단어를 넣어 보세요. 한국어라면 <code>사자 조용히</code>, 영어라면 <code>brave lion</code>입니다. 모든 문장에 두 단어가 들어가며, <code>brave</code>는 남은 자리에 따라 수식어가 되기도 하고 서술어가 되기도 합니다.</li><li><code>shape</code>를 <code>simple</code>에서 <code>complex</code>로 바꿔 보세요. 단어가 길어지는 대신 구가 하나 늘어납니다. <code>minLength</code>가 글자 단위로 하는 일을 구 단위로 하는 셈입니다.</li></ul><h2 id="what-this-page-is-not" tabindex="-1">이 페이지의 범위 <a class="header-anchor" href="#what-this-page-is-not" aria-label="Permalink to &quot;이 페이지의 범위 {#what-this-page-is-not}&quot;">​</a></h2><p>이 페이지는 시연이며, 브라우저에서 대량으로 호출하라고 만든 것이 아닙니다. 라이브러리 자체는 네트워크 호출도 의존성도 없으므로, 여기서 도는 것과 똑같은 코드가 서버에서도 빌드 스크립트에서도 테스트 픽스처에서도 그대로 돕니다.</p><p>결과에는 <strong>시드가 없습니다.</strong> 같은 묶음을 다시 받을 방법이 없으므로, 재현이 필요하다면 설정이 아니라 생성된 문자열을 보관하세요.</p><h2 id="where-to-go-next" tabindex="-1">다음으로 볼 것 <a class="header-anchor" href="#where-to-go-next" aria-label="Permalink to &quot;다음으로 볼 것 {#where-to-go-next}&quot;">​</a></h2><ul><li><a href="./guide/getting-started">시작하기</a> — 세 패키지 중 쓰는 것으로 설치하기.</li><li><a href="./name/rand-name"><code>randName</code></a>, <a href="./nickname/rand-nickname"><code>randNickname</code></a>, <a href="./word/rand-word"><code>randWord</code></a>, <a href="./sentence/rand-sentence"><code>randSentence</code></a> — 위 패널에 있는 모든 옵션의 설명.</li><li><a href="./guide/languages">지원 언어</a> — 언어마다 할 수 있는 것과 할 수 없는 것.</li></ul>',7))])}const x=c(s,[["render",m]]);export{k as __pageData,x as default};
